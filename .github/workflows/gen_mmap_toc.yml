name: gen_mmap_toc
on:
  push:
    branches: [ master ]
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: gen_mmap_toc
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4